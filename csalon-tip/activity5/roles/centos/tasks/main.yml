---
# tasks file for roles/centos

- name: java in centos
  yum:
    name: java-11-openjdk-devel
    state: latest

- name: ansbile, boto, and openstack
  pip:
    name:
    - ansible
    - boto
    - python-openstackclient
