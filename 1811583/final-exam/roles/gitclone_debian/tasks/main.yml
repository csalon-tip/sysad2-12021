---
# tasks file for roles/gitclone_debian
- name: Install git on debian
  apt:
   name: git
   state: latest

- name: Clone Prelim Exam branch on debian
  git:
    repo: '{{ prelim }}'
    dest: ./prelim-exam
    version: prelim-exam
